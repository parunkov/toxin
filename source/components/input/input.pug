mixin input(h3, type, name, cl, val, plac, p, icon, color)
	.input__container
		h3.input__h3= h3
		p.input__p= p		
		input(type= type name= name class="input__input "+cl value= val placeholder= plac)
		if icon
			.input__icon
				if color === 'purple'
					i.input__icon-text.input__icon-text_purple= icon
				else
					i.input__icon-text= icon