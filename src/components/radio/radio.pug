mixin radio(cl, text)
	.radio
		each t, i in text
			label.radio__label
				if i === 0
					input(type="radio" name=cl class="radio__input"+" "+cl checked)
				else
					input(type="radio" name=cl class="radio__input"+" "+cl)
				.radio__custom
				p.radio__text= t