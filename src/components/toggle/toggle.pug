mixin toggle(data)
	-
		const {text} = data;
	.toggle
		each value, i in text
			label.toggle__label
				input(type="checkbox" name="toggle" class="toggle__input js-toggle__input")
				.toggle__custom
					.toggle__custom-inner
				p.toggle__text= value