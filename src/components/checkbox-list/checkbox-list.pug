include ../../components/checkbox/checkbox.pug

mixin checkbox-list(data = {})
	-
		const {text, title = null} = data
	.checkbox-list
		each value, i in text
			-
				let header = '';
				if (title) {
					header = title[i];
				}
				const text1 = text[i];
			+checkbox(i, header, text1)
